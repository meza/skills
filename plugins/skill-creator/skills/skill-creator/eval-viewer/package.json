{
  "name": "eval-viewer",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && npm run build:client && npm run build:server",
    "build:client": "vite build",
    "build:server": "vite build --config vite.server.config.ts",
    "biome:fix": "biome check --write .",
    "coverage": "vitest run --coverage",
    "doctor": "react-doctor --verbose --full --lint --no-dead-code --fail-on warning",
    "dev": "npm run dev:server",
    "dev:server": "npm run build && npm run fixture:visual && tsx watch --clear-screen=false scripts/devServer.ts .tmp/visual-fixture",
    "fixture:visual": "tsx tests/fixtures/writeVisualFixture.ts",
    "serve": "node dist/server/main.js",
    "serve:visual": "npm run build && npm run fixture:visual && npm run serve -- .tmp/visual-fixture",
    "style:check": "stylelint \"src/client/**/*.css\"",
    "test": "vitest run",
    "test:package": "vitest run tests/packagedViewer.test.ts",
    "test:visual": "playwright test",
    "test:visual:docker": "docker run --rm -t -v \".:/work\" -v \"/work/node_modules\" -v \"../schemas:/schemas:ro\" -w /work mcr.microsoft.com/playwright:v1.62.0-noble /bin/bash -lc \"npm ci && npm run test:visual\"",
    "test:visual:update": "run-s test:visual:update:local test:visual:update:docker",
    "test:visual:update:local": "playwright test --update-snapshots changed",
    "test:visual:update:docker": "docker run --rm -t -v \".:/work\" -v \"/work/node_modules\" -v \"../schemas:/schemas:ro\" -w /work mcr.microsoft.com/playwright:v1.62.0-noble /bin/bash -lc \"npm ci && npm run test:visual:update:local\"",
    "validate": "run-s \"biome:fix -- --error-on-warnings\" style:check coverage build"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Edge versions",
    "last 2 Firefox versions",
    "last 2 Safari versions"
  ],
  "dependencies": {
    "@fastify/static": "^8.3.0",
    "@vitejs/plugin-react": "6.0.5",
    "ajv": "8.20.0",
    "fastify": "5.11.2",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "vite": "8.2.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@meza/biome": "1.3.9",
    "@playwright/test": "1.62.1",
    "@testing-library/jest-dom": "7.0.0",
    "@testing-library/react": "16.3.2",
    "@testing-library/user-event": "14.6.3",
    "@types/node": "24.13.3",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitest/coverage-v8": "4.1.10",
    "jsdom": "30.0.1",
    "memfs": "4.68.0",
    "npm-run-all2": "9.0.3",
    "postcss-preset-env": "11.3.2",
    "stylelint": "17.14.1",
    "stylelint-config-standard": "40.0.0",
    "tsx": "4.23.9",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": "24"
  },
  "allowScripts": {
    "esbuild@0.28.0": true
  }
}
