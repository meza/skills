All temporary artifacts (eval runs, fixtures, workspaces, intermediate outputs) must go in the `tmp/` folder at the project root. Do not create temporary directories anywhere else in the tree.

All skill evaluations MUST use the Sonnet model with medium effort. No exceptions.
